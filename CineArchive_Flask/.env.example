# --------------------------------------------
# CineArchive Project - Environment Variables
# --------------------------------------------
# Copy this file as `.env` and fill in your own values.
# This file provides configuration for MySQL connection.
# Do NOT commit your real `.env` file to version control.
# Make sure the .gitignore file has .env listed in it

# MySQL database host (usually localhost for local development)
DB_HOST=localhost

# MySQL username (e.g., 'root' or your DB user)
DB_USER=root

# MySQL password (set your own local password)
DB_PASSWORD=yourpassword

# Name of the MySQL database you created for the project
DB_NAME=CineArchive


